services:
  app-prod:
    container_name: gv-prod
    image: gv-prod
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - 3040:3001
